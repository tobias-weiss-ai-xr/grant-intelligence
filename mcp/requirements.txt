# Runtime-Abhängigkeiten des MVP (mcp/). Install: pip install -r requirements.txt
mcp>=1.0,<2
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27


# Dev-Abhängigkeiten (Tests, Linting, Typprüfung):
# pip install -r requirements-dev.txt
